consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: object
      required:
        - db_name
      properties:
        db_name:
          type: string
responses:
  200:
    description: Database deleted

