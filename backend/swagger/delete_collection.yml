consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: object
      required:
        - db_name
        - collection_name
      properties:
        db_name:
          type: string
        collection_name:
          type: string
responses:
  200:
    description: Collection deleted

